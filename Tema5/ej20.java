import java.util.Scanner;
public class ej20 { //Pintar una pirámide hueca introduciendo altura y carácter
  public static void main (String[] args) {
    Scanner r = new Scanner(System.in);
    int altura;
    System.out.println("Introduce el carácter con el que quieres pintar la pirámide");
    String x = System.console().readLine();
    System.out.println("Introduce la altura");
    altura = r.nextInt();
    alturah = alturah;
    for (int vuelta = 0; vuelta <= altura; altura--) {
      for (int espacios = altura-1; espacios >= 0; espacios--) {
        System.out.print(" ");
      }
      if (altura < alturah) {
        System.out.print(x);
      } else {
        System.out.println(x);
      }
      for (int espacios2 = altura/3*2-espacios+1; espacios2 //espacios de dentro
      
    }
  }
}
